<h1>Pilot Records</h1>

<% @pilots.each do |pilot| %>
  <h2>Pilot Name:<%=pilot.name%></h2>
  <h2>Is A Captain:<%=pilot.is_captain%></h2>
  <h2>Years Of Experience:<%=pilot.years_experience%></h2>
  <%= link_to "Update Pilot", "/pilots/#{pilot.id}/edit"%>
<% end %>